<div class="favorites-page">
    <h1 class="favorites-title">Обрані рецепти</h1>
    
    <?php if (empty($favorites)): ?>
        <div class="favorites-empty">
            <p>У вас поки що немає обраних рецептів</p>
            <a href="catalog.php" class="cta-button">Перейти до каталогу</a>
        </div>
    <?php else: ?>
        <div class="favorites-list">
            <?php foreach ($favorites as $recipe): ?>
                <div class="favorites-item">
                    <div class="favorites-item-img">
                        <img src="<?= htmlspecialchars($recipe['image']) ?>" alt="<?= htmlspecialchars($recipe['name']) ?>">
                    </div>
                    <div class="favorites-item-content">
                        <h3 class="favorites-item-title"><?= htmlspecialchars($recipe['name']) ?></h3>
                        <div class="favorites-item-meta">
                            <span><?= htmlspecialchars($recipe['category_name']) ?></span>
                            <span><?= $recipe['prep_time_min'] + $recipe['cook_time_min'] ?> хв</span>
                        </div>
                        <div class="favorites-item-actions">
                            <a href="recipe.php?id=<?= $recipe['id'] ?>" class="favorites-view-btn">Рецепт →</a>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
    <?php endif; ?>
</div>